'use strict';

// Initialize our todos data
// What data type should you use?
let todos = '????';

// Save your element references
const todosWrapper = document.getElementById('todos-wrapper');
const input = document.getElementById('todoTitle');
const btn = document.getElementById('addTodoBtn');

// When page full loads render the todos
document.addEventListener('DOMContentLoaded', );

// When user clicks add todo btn add a new todo object to our todos array
// Don't forget to render the new updated todos array 
btn.addEventListener('click', () => {

});

// Use only 1 event to handle todo item click
// Toggle todo completed property
todosWrapper.addEventListener('click', e => {
  
});

// Render the todo items of the todos array
function renderTodos() {
  
}

// Create the html elements for a single todo item
// It should return the new item element
function generateTodoElement(todo) {
  
}